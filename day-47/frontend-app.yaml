apiVersion: v1
kind: Pod
metadata:
  name: frontend-app
  labels:
    app: frontend-app
spec:
  containers:
  - name: frontend-app
    image: nginx           # you can use any app image
    ports:
    - containerPort: 8080

