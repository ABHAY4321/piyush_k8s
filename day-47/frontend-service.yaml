apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
spec:
  selector:
    app: frontend-app
  ports:
  - name: http
    port: 80          # service port
    targetPort: 8080 # pod container port

